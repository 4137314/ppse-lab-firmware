[platformio]
default_envs = pico
src_dir = .
include_dir = include
build_dir = .firm-build/build
libdeps_dir = .firm-build/libdeps

[common]
build_flags = 
    -Os
    -Dtimegm=mktime
    -DDEBUG=0
    -lc -lm
    # Nota: FASTLED_INTERNAL rimosso perché già definito dalla lib
    -DFASTLED_NO_CUSTOM_RED_BLACK_TREE
    -DFASTLED_RP2040_CLOCKLESS_PIO
    -DSERIAL_MAX_SETUP_TIME=5000
    -DPIO_FRAMEWORK_ARDUINO_ENABLE_RTTI
    -DPIO_FRAMEWORK_ARDUINO_ENABLE_EXCEPTIONS

[env]
framework = arduino
monitor_speed = 115200
monitor_port = /dev/ttyACM?
lib_deps = 
    adafruit/Adafruit BusIO @ ^1.17.4
    adafruit/Adafruit GFX Library @ ^1.12.5
    adafruit/Adafruit SSD1306 @ ^2.5.16
    adafruit/Adafruit NeoPixel @ ^1.15.4
    fastled/FastLED @ 3.9.4

[env:pico]
platform = https://github.com/maxgerhardt/platform-raspberrypi.git@1.18.0
board = pico
board_build.core = earlephilhower
board_build.usb_stack = tinyusb
board_build.filesystem = fatfs
board_build.filesystem_size = 0.5m
board_build.maximum_size = 2097152
# --- FIX CRITICO: Compila solo il tuo codice, ignora gli esempi delle lib ---
build_src_filter = +<main.cpp> +<src/*.cpp>
# ---------------------------------------------------------------------------
build_flags = ${common.build_flags}
upload_protocol = picotool

platform_packages = 
    toolchain-rp2040-earlephilhower
    earlephilhower/tool-mklittlefs-rp2040-earlephilhower@^5.100300.230216
